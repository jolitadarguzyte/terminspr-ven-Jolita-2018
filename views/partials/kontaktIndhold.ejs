<div class="shalia">


    <div class="cykelKatagori">
        <% kontakt_oplysninger.forEach(function(kontakt) { %>

            <div>
                <h4>
                    <%= kontakt.navn %>
                </h4>
                <P>
                    <%= kontakt.gade %>
                </p>
                <P>
                    <%= kontakt.by_post %>
                </p>
                <br>
                <P>
                    <%= kontakt.tlf %>
                </p>
                <P>
                    <%= kontakt.fax %>
                </p>
                <br>
                <P>
                    <%= kontakt.email %>
                </p>
                <img class="img1" src="images/<%= kontakt.url %>" alt="<%= kontakt %>">
            </div>

            <% }) %>
    </div>

    <div class="indhold3">
        <% tilbud.forEach(function(element) { %>
            <div class="flex-container">

                <div>
                    <p id="navn">
                        <%= element.navn %>
                    </p>
                    <img src="/images/<%=element.img %>">
                    <p id="pris">
                        <%= element.førPris %>
                    </p>
                    <p id="pris1">
                        <%= element.nupris %>
                    </p>
                </div>
            </div>

            <% }); %>
    </div>
</div>